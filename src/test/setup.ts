import { beforeAll, afterAll, afterEach } from 'vitest'

// Setup for all tests
beforeAll(() => {
  // Global test setup
})

afterEach(() => {
  // Cleanup after each test
})

afterAll(() => {
  // Global test cleanup
})